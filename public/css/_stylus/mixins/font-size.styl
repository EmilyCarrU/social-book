font-size(n = $base-font-size)
	if unit(n) is undefined
		font-size: n
	if unit(n) is 'px'
		font-size: n
		font-size: (remove-unit(n) / $default-font-size) rem
	else if unit(n) is 'rem'
		font-size: (remove-unit(n) * $default-font-size) px
		font-size: n
	else
		font-size: n